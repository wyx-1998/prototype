
## 方案概述

### 核心理念

本方案采用**Chat中心的数据接入模式**，将数据选择、预览、确认的全流程整合到对话界面中，实现"对话即操作"的自然交互体验。用户无需在多个界面间切换，通过自然语言即可完成复杂的数据关联和内容生成任务。

### 设计原则

| 原则 | 说明 | 价值 |
| --- | --- | --- |
| **画布零打扰** | 画布仅展示最终生成内容，不显示数据选择控件或中间状态 | 保持文档视觉的专业性和简洁性 |
| **对话式接入** | 所有数据操作通过Chat对话完成，支持自然语言描述需求 | 降低学习成本，符合用户自然思维 |
| **智能推荐** | AI主动分析需求并推荐相关数据源，用户只需确认 | 提升效率，减少用户搜索成本 |
| **状态可见** | 已接入数据在Chat中持久化展示，支持随时查看和调整 | 增强操作透明度和可控性 |
| **快速撤销** | 支持一键移除已接入数据，重新选择 | 提供容错机制，降低试错成本 |

### 架构对比

#### 旧方案（画布中心）

```plaintext
用户操作流程：
顶部工具栏[+ 插入数据] 
  ↓ 点击
弹出数据选择器（覆盖画布）
  ↓ 选择完成
画布插入数据卡片（占用版面）
  ↓ 卡片就绪
Chat基于卡片生成内容
  ↓ 生成完成
手动删除数据卡片
```

**问题：**

- 打断编辑流程，需要频繁切换焦点

- 数据卡片占用画布空间，影响视觉呈现

- 数据和内容混杂，难以区分最终产出

- 操作步骤冗长，需要多次点击

#### 新方案（Chat中心）

```plaintext
用户操作流程：
Chat输入需求（如"生成培训课件"）
  ↓ AI理解意图
AI推荐相关数据源（在Chat中展示）
  ↓ 用户确认
Chat内预览数据（可折叠卡片）
  ↓ 确认无误
AI直接生成内容插入画布（干净的文档）
  ↓ 完成
数据引用以脚注形式保留，可溯源
```

**优势：**

- 单一交互界面，无需切换

- 画布始终保持专业文档外观

- 数据接入和内容生成一气呵成

- 支持自然语言操作，更符合人类思维

---

## 四种作品类型详细交互设计

### Type-1: 培训课件 (Courseware)

#### 典型应用场景

**场景描述：** 企业安全培训讲师需要制作"高处作业安全培训"课件，要求内容基于企业真实事故案例，引用最新法规标准，并包含标准作业流程。

**用户角色：** 安全培训讲师\
**完成时间：** 传统方式需60-90分钟，新方案可缩短至15-20分钟\
**核心痛点：** 需要从多个系统收集案例、法规、流程文档，手动整理汇总

#### Chat完整交互流程

```plaintext
═══════════════════════════════════════════════
第一步：用户发起需求
═══════════════════════════════════════════════

👤 用户：
帮我生成一份高处作业安全培训课件

🤖 AI响应：
好的，我将为您生成课件。为了内容更贴合实际，建议引入以下数据：

📊 可接入的数据源：
├─ 🔴 事故案例库（检索到 8 条高处作业相关案例）
├─ 📋 作业流程库（高处作业标准流程 SOP）
├─ 📜 法规库（相关安全规范 5 条）
└─ 🎬 培训素材库（视频 3 个、图片 12 张）

请问需要引入哪些数据？您可以：
• 回复序号选择，如"1,2,3"
• 或直接说"全部引入"
• 或说"不需要，直接生成"

═══════════════════════════════════════════════
第二步：用户选择数据源
═══════════════════════════════════════════════

👤 用户：
引入事故案例和法规

🤖 AI响应：
✅ 正在加载数据...

📦 已接入数据：

【事故案例库 - 8条】
┌─────────────────────────────────────┐
│ ✓ 2023-05-12 某车间坠落事故         │
│   位置：2号车间二层  伤亡：1人重伤  │
│                                     │
│ ✓ 2024-02-03 外包施工高空坠物       │
│   位置：3号厂房外墙  伤亡：2人轻伤  │
│                                     │
│ ✓ 2024-08-19 脚手架倾覆事件         │
│   位置：新建项目工地  伤亡：无       │
│                                     │
│ ... 还有 5 条案例                   │
│ [展开全部] [筛选调整]                │
└─────────────────────────────────────┘

【法规库 - 5条】
┌─────────────────────────────────────┐
│ ✓ GB/T 3608-2008 高处作业分级       │
│ ✓ 安全生产法 第四十一条              │
│ ✓ 建筑施工高处作业安全技术规范       │
│ ... 还有 2 条                       │
│ [展开全部]                           │
└─────────────────────────────────────┘

💡 数据已准备就绪，现在为您生成课件...

═══════════════════════════════════════════════
第三步：内容生成并插入画布
═══════════════════════════════════════════════

（3秒后）

🤖 AI响应：
✨ 课件已生成！已自动插入到画布中。

课件结构：
• 第1章 高处作业定义与分级（引用 GB/T 3608）
• 第2章 法律法规要求（引用安全生产法）
• 第3章 典型事故警示（基于 8 个真实案例）
• 第4章 标准作业流程
• 第5章 考核要求

您可以：
• 说"重新生成第3章"调整某个章节
• 说"增加视频素材"补充内容
• 直接在画布中编辑
```

#### 数据源类型和展示方式

| 数据源类型 | 来源系统 | 展示形式 | 关键字段 |
| --- | --- | --- | --- |
| 事故案例库 | 事故管理系统 | 可折叠列表卡片 | 时间、地点、伤亡情况、原因分析 |
| 法规库 | 法规知识库 | 条款列表 | 法规名称、条款编号、条款内容 |
| 作业流程库 | 流程管理系统 | 流程图预览 | 流程名称、步骤数、适用范围 |
| 培训素材库 | 资源管理系统 | 缩略图网格 | 文件名、类型、时长/大小 |

#### 特殊交互设计点

**1. 智能筛选建议**

当用户选择"事故案例库"后，AI自动分析：

```plaintext
🤖 AI提示：
检测到您选择了 8 条案例，建议按以下维度筛选：

📊 伤亡程度分布：
  • 重伤及以上：3 条（建议保留）
  • 轻伤：3 条（建议保留 1-2 条）
  • 无伤亡：2 条（建议移除）

💡 建议保留 4-5 条典型案例，避免课件过长

[应用建议] [保持原选择]
```

**2. 法规条款精准定位**

```plaintext
用户点击"展开法规库"后：

【安全生产法 第四十一条】
┌─────────────────────────────────────┐
│ 原文内容：                          │
│ "生产经营单位应当建立安全生产教育和│
│  培训档案，如实记录安全生产教育和培│
│  训的时间、内容、参加人员..."      │
│                                     │
│ 💡 应用建议：                       │
│ 此条款适合插入"第5章 考核要求"中，│
│ 作为培训档案管理的法律依据。        │
│                                     │
│ [插入到指定位置] [查看完整条款]    │
└─────────────────────────────────────┘
```

**3. 数据实时更新提醒**

```plaintext
当用户在编辑过程中，相关数据源有更新时：

🔔 数据更新提醒：
事故案例库有 1 条新增记录：
• 2025-12-20 高空坠物险情事件

[更新课件内容] [稍后处理]
```

---

### Type-2: 标准公文 (Standard Doc)

#### 典型应用场景

**场景描述：** 安全管理部门需要编写"2025年度安全生产责任制"，要求引用最新法规，参考历史文档框架，并同步最新组织架构信息。

**用户角色：** 安全主管、行政管理人员\
**完成时间：** 传统方式需120-180分钟，新方案可缩短至30-40分钟\
**核心痛点：** 需要查阅大量法规文件，从HR系统获取组织架构，参考多份历史文档

#### Chat完整交互流程

```plaintext
═══════════════════════════════════════════════
第一步：用户发起需求
═══════════════════════════════════════════════

👤 用户：
写一份2025年度安全生产责任制

🤖 AI响应：
收到！正在为您检索相关数据...

🔍 智能匹配结果：

1️⃣ 历史文档参考
   • 2024年度安全生产责任制（可继承框架）
   • 2023年隐患排查管理制度（可复用条款）

2️⃣ 组织架构数据
   • 当前企业组织架构（最后更新：2025-12-01）
   • 已识别关键岗位：总经理、安全总监、车间主任...

3️⃣ 法规依据
   • 《安全生产法》（2021修订版）相关条款 12 条
   • GB 33000-2020 标准要求

---

💬 建议方案：
A. 基于 2024 年文档更新（快速生成，保留已有结构）
B. 全新编写（完全重构，更规范但耗时较长）

您选择哪个方案？或者告诉我具体要求。

═══════════════════════════════════════════════
第二步：用户选择方案并提出需求
═══════════════════════════════════════════════

👤 用户：
选 A，但要补充最新的法规

🤖 AI响应：
好的！正在处理...

📥 数据接入进度：
✅ 已加载 2024 年责任制文档
✅ 已同步最新组织架构
✅ 正在匹配 2024-2025 新增法规...

找到 3 条新增/修订法规：
┌─────────────────────────────────────┐
│ 🆕 《安全生产法实施条例》(2024.5)  │
│    • 新增第23条：主要负责人职责细化│
│    • 修订第45条：安全投入标准提高  │
│                                     │
│ 🆕 GB 42590-2024 危化品管理规范    │
│    • 新增重大危险源分级标准        │
│                                     │
│ 📝 《刑法修正案》涉安全生产条款    │
│    • 重大责任事故罪量刑标准调整    │
│                                     │
│ [查看详情] [全部应用]                │
└─────────────────────────────────────┘

═══════════════════════════════════════════════
第三步：展示差异对比和生成结果
═══════════════════════════════════════════════

🤖 AI响应：
✨ 文档已生成！

📊 与 2024 年版本对比：

更新内容：
• 1.1 编制依据 - 新增 3 条法规引用
• 2.2 总经理职责 - 根据新条例补充 2 项职责
• 2.3 安全总监职责 - 新增应急管理职责描述
• 3.4 安全投入保障 - 更新投入标准（由营收2%提升至2.5%）
• 附录 B - 新增法律责任清单

保持不变：
• 整体框架结构（6章 + 3附录）
• 车间级、班组级责任描述
• 考核评分标准

您可以说：
• "展开第2.3节内容"查看具体条款
• "对比去年版本"查看变更明细
• "导出审批版"生成带修订标记的版本
```

#### 数据源类型和展示方式

| 数据源类型 | 来源系统 | 展示形式 | 关键字段 |
| --- | --- | --- | --- |
| 历史文档库 | 文档管理系统 | 文档预览卡片 | 文档标题、版本号、生效日期 |
| 组织架构 | HR系统/OA | 树形结构图 | 部门名称、负责人、岗位职责 |
| 法规库 | 法规知识库 | 条款对比表 | 法规名称、新增/修订标识、生效日期 |
| 历史修订记录 | 版本控制系统 | 时间轴 | 修订时间、修订人、修订内容摘要 |

#### 特殊交互设计点

**1. 法规更新智能提醒**

```plaintext
🔔 检测到法规变更影响：

当前文档引用了《安全生产法》第21条，该条款已于
2024年5月修订，可能影响以下章节：

⚠️ 影响范围：
• 第2章 组织机构与职责 - 主要负责人职责描述
• 第3章 安全管理制度 - 会议制度要求
• 附录A - 职责清单

建议操作：
[对比新旧条款] [全文更新] [仅标记提醒]
```

**2. 组织架构同步确认**

```plaintext
📋 组织架构变更提醒：

与 2024 年版本相比，组织架构发生以下变化：

新增部门：
✓ 新能源事业部（负责人：张三）

撤销部门：
✗ 第三生产车间（已并入第二车间）

岗位调整：
• 安全总监：李四 → 王五
• 设备部经理：赵六 → 孙七

💡 建议同步更新文档中的相关章节
[应用全部变更] [逐项确认]
```

**3. 条款精准插入**

```plaintext
用户说："补充一条关于安全培训的法律依据"

🤖 AI响应：
🔍 为您检索到 5 条相关法规条款：

推荐度 ★★★★★
【安全生产法 第二十八条】
┌─────────────────────────────────────┐
│ "生产经营单位应当对从业人员进行安│
│  全生产教育和培训，保证从业人员具│
│  备必要的安全生产知识..."          │
│                                     │
│ 💡 适用场景：                       │
│ 此条款是安全培训的基础法律依据，建│
│ 议插入"第4章 教育培训管理"章节开头│
│                                     │
│ [插入此条款]                         │
└─────────────────────────────────────┘

推荐度 ★★★★☆
【安全生产法 第三十条】
（培训档案管理要求）
[查看详情]

推荐度 ★★★☆☆
【GB 33000-2020 条款6.2】
（培训计划制定要求）
[查看详情]
```

---

### Type-3: 业务单据 (Business Form)

#### 典型应用场景

**场景描述：** 安全员在日常巡检中发现隐患，需要立即生成"隐患整改通知书"并发送给责任人，要求快速、准确、可追溯。

**用户角色：** 一线安全员、巡检人员\
**完成时间：** 传统方式需10-15分钟，新方案可缩短至1-2分钟\
**核心痛点：** 需要手动录入隐患信息、查找责任人联系方式、上传照片，流程繁琐且容易出错

**⚡️ 高频场景特点：**

- 使用频率极高（日均可能需生成10+份）

- 对速度要求极高（现场发现隐患需立即处理）

- 需要移动端友好（现场操作为主）

- 需支持语音输入（解放双手）

#### Chat完整交互流程（极简模式）

```plaintext
═══════════════════════════════════════════════
第一步：语音快速发起
═══════════════════════════════════════════════

👤 用户：（语音输入）
生成隐患整改通知书，刚才巡检发现的

🤖 AI响应：
明白！正在调取您最近的巡检记录...

🔍 找到 2 条待处理隐患：

┌─────────────────────────────────────┐
│ 📋 10:15 仓库区域巡检               │
│ 隐患：消防器材被货物遮挡            │
│ 位置：3号仓库西侧疏散通道           │
│ 等级：⚠️ 一般隐患                   │
│ 责任人：仓储部 李明                 │
│ 📷 照片：2 张                       │
│ [选择此条] ←                        │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ 📋 08:30 1号车间巡检                │
│ 隐患：电气线路老化                  │
│ 位置：1号车间东侧配电箱             │
│ 等级：🔴 重大隐患                   │
│ 责任人：设备部 张伟                 │
│ 📷 照片：3 张                       │
│ [选择此条]                           │
└─────────────────────────────────────┘

请问是哪一条？或者说"全部生成"

═══════════════════════════════════════════════
第二步：用户确认
═══════════════════════════════════════════════

👤 用户：（语音）
第一条

🤖 AI响应：
✅ 数据已锁定

📦 自动关联信息：
• 责任人：李明（工号:1234, 手机:138****5678）
• 部门领导：仓储部经理 王芳
• 整改标准：参照《消防设施管理规定》3.2 条
• 建议整改期限：3 个工作日（基于隐患等级）
• 验收人：安全员 您本人

═══════════════════════════════════════════════
第三步：内容生成并提供快捷操作
═══════════════════════════════════════════════

（1秒后）

🤖 AI响应：
🚀 通知书已生成并插入画布！

文号：YH-2025-1221-001

📋 内容摘要：
致：仓储部 李明
隐患：消防器材被货物遮挡
位置：3号仓库西侧疏散通道
整改期限：2025-12-24 前
验收人：安全员（您）

快捷操作：
┌─────────────────────────────────────┐
│ 📤 [发送到企业微信]                 │
│ 📧 [发送邮件通知]                   │
│ 📋 [生成二维码（现场扫码确认）]    │
│ 🖨️ [打印纸质版]                     │
│                                     │
│ 或继续对话：                        │
│ • 说"修改整改期限为5天"            │
│ • 说"添加抄送人"                    │
│ • 说"再加一条隐患"合并生成          │
└─────────────────────────────────────┘

═══════════════════════════════════════════════
第四步：一键分发
═══════════════════════════════════════════════

👤 用户：（点击或语音）
发送到企业微信

🤖 AI响应：
✅ 已通过企业微信发送给：
• 李明（责任人）- 待确认
• 王芳（部门领导）- 待知晓

📱 同时已生成任务卡片，对方可直接：
• 查看现场照片
• 提交整改计划
• 上传整改照片
• 申请验收

💬 您可以说"查看发送状态"实时跟踪
```

#### 数据源类型和展示方式

| 数据源类型 | 来源系统 | 展示形式 | 关键字段 | 实时性要求 |
| --- | --- | --- | --- | --- |
| 巡检记录 | 巡检管理系统 | 时间轴列表 | 时间、位置、隐患描述、照片 | 实时同步 |
| 人员信息 | HR系统 | 信息卡片 | 姓名、工号、部门、联系方式 | 每日同步 |
| 隐患标准库 | 标准管理系统 | 条款引用 | 隐患分类、等级标准、整改要求 | 按需查询 |
| 整改期限规则 | 规则引擎 | 自动计算 | 隐患等级→建议期限映射 | 实时计算 |

#### 特殊交互设计点

**1. 智能预判和自动填充**

```plaintext
当用户说"生成隐患通知书"时，AI自动执行：

✓ 定位用户身份（当前登录的安全员）
✓ 查询最近2小时内的巡检记录
✓ 筛选"待处理"状态的隐患
✓ 按时间倒序排列
✓ 自动匹配责任人信息
✓ 预计算整改期限
✓ 生成唯一文号

用户无需手动输入任何信息，只需确认选择
```

**2. 语音交互优化**

```plaintext
支持的语音指令：

快速生成类：
• "隐患通知书"
• "整改通知"
• "刚才那个"（指代最近一条巡检）

修改调整类：
• "期限改成5天"
• "换个责任人"
• "加上部门领导"

发送分发类：
• "发出去"（默认发企业微信）
• "打印"
• "发邮件"
```

**3. 移动端适配**

```plaintext
📱 移动端Chat界面特殊设计：

┌─────────────────────┐
│  🎤 按住说话         │ ← 大按钮，方便现场操作
├─────────────────────┤
│ [10:15 仓库巡检] ✓  │ ← 卡片横向滑动选择
│ [08:30 车间巡检]    │
├─────────────────────┤
│ 📤 快捷发送          │ ← 一键发送，无需二次确认
│ 📋 查看详情          │
└─────────────────────┘
```

**4. 现场照片快速关联**

```plaintext
当用户在巡检时拍照后：

🤖 AI自动提示：
检测到您刚刚拍摄了 2 张照片，是否关联到当前
巡检记录？

📷 照片预览：
[img] [img]

[自动关联] [稍后处理]

（点击"自动关联"后）

✅ 照片已添加到巡检记录
   当生成通知书时将自动插入
```

**5. 批量生成优化**

```plaintext
当用户说"全部生成"时：

🤖 AI响应：
检测到 2 条待处理隐患，将分别生成通知书：

📋 生成计划：
1. YH-2025-1221-001 → 仓储部 李明
2. YH-2025-1221-002 → 设备部 张伟

[确认批量生成]

（生成后）

✅ 2 份通知书已生成并发送
• 李明 - 已送达 ✓
• 张伟 - 已送达 ✓

💬 您可以说"查看全部通知"统一管理
```

---

### Type-4: 考试试卷 (Examination)

#### 典型应用场景

**场景描述：** 培训管理员需要为"电工作业安全培训"生成考试试卷，要求从题库中智能抽题，覆盖关键知识点，难度适中，并包含最新的锂电池安全内容。

**用户角色：** 培训管理员、教育专员\
**完成时间：** 传统方式需30-45分钟，新方案可缩短至3-5分钟\
**核心痛点：** 手动从题库筛选题目，需要考虑知识点覆盖、难度分布、题型配比，工作量大且容易遗漏

#### Chat完整交互流程

```plaintext
═══════════════════════════════════════════════
第一步：用户发起需求
═══════════════════════════════════════════════

👤 用户：
生成一份电工作业安全考试试卷

🤖 AI响应：
收到！正在访问题库...

📚 题库状态：
• 电工作业安全题库：共 326 题
  ├─ 单选题：156 题
  ├─ 多选题：98 题
  ├─ 判断题：72 题
  └─ 最后更新：2025-11-15

🔍 知识点分布：
• 基础理论：28%
• 设备操作：35%
• 应急处置：22%
• 法律法规：15%

---

💬 请告诉我组卷要求：

快捷选项：
1️⃣ 标准模式（单选20+多选10+判断10, 100分, 60分钟）
2️⃣ 快速测验（单选10+判断10, 50分, 30分钟）
3️⃣ 自定义配置

或者直接说出您的要求，如：
"30道单选，难度中等，侧重实操"

═══════════════════════════════════════════════
第二步：用户提出具体要求
═══════════════════════════════════════════════

👤 用户：
标准模式，但要包含最新的锂电池安全内容

🤖 AI响应：
好的！正在智能筛选...

🔍 检索到"锂电池安全"相关题目：
• 单选题：8 题（新增于 2024-09）
• 多选题：5 题
• 判断题：3 题

📊 组卷策略：
• 锂电池专题占比 20%（新规要求）
• 传统电工安全占比 60%
• 应急处置占比 20%
• 难度分布：简单 30% | 中等 50% | 困难 20%

📋 知识点覆盖计划：
┌─────────────────────────────────────┐
│ ✓ 锂电池充电安全规范（4题）         │
│ ✓ 热失控应急处置（3题）             │
│ ✓ 传统电气作业规范（10题）          │
│ ✓ 个人防护用品使用（5题）           │
│ ✓ 触电急救（4题）                   │
│ ✓ 安全用电法律法规（4题）           │
│ ✓ 其他知识点（10题）                │
└─────────────────────────────────────┘

[开始生成] [调整策略]

═══════════════════════════════════════════════
第三步：展示题目预览并支持调整
═══════════════════════════════════════════════

🤖 AI响应：
✨ 试卷已生成！

📋 试卷信息：
• 试卷编号：EX-2025-1221-001
• 总题数：40 题
• 总分：100 分
• 建议时长：60 分钟
• 包含知识点：16 个

📊 题目分布：
• 单选题：20 题（每题 2 分）
• 多选题：10 题（每题 3 分）
• 判断题：10 题（每题 1 分）

🔍 题目预览（前3题）：

【单选题 第1题】难度：★☆☆
┌─────────────────────────────────────┐
│ 锂电池充电时，应在什么环境下进行？ │
│ A. 密闭空间                          │
│ B. 通风良好场所                      │
│ C. 潮湿环境                          │
│ D. 高温场所                          │
│                                     │
│ 🔗 来源：锂电池安全题库 > 基础知识  │
│ 📊 历史正确率：92%                  │
│ [替换此题] [查看解析]                │
└─────────────────────────────────────┘

【单选题 第2题】难度：★★☆
┌─────────────────────────────────────┐
│ 发现锂电池鼓包时，应首先采取什么措│
│ 施？                                │
│ A. 立即充电                          │
│ B. 继续使用                          │
│ C. 停止使用并隔离                    │
│ D. 放置在高温处加速反应              │
│                                     │
│ 🔗 来源：应急处置题库 > 热失控处置  │
│ 📊 历史正确率：78%                  │
│ [替换此题]                           │
└─────────────────────────────────────┘

... 还有 38 题

[展开全部题目] [调整某题] [重新生成]

═══════════════════════════════════════════════
第四步：支持灵活调整
═══════════════════════════════════════════════

👤 用户：
第1题太简单了，换一道

🤖 AI响应：
好的，为您推荐替换题目：

🔄 替换候选（同知识点，更高难度）：

推荐题 ★★☆
┌─────────────────────────────────────┐
│ 锂电池储能系统发生火灾时，不宜使用│
│ 哪种灭火介质？                      │
│ A. 干粉灭火器                        │
│ B. 水基灭火器                        │
│ C. CO2灭火器                         │
│ D. 泡沫灭火器                        │
│                                     │
│ 📊 历史正确率：65%（难度适中）      │
│ [使用此题]                           │
└─────────────────────────────────────┘

备选题 ★★★
（更高难度选项）
[查看备选]
```

#### 数据源类型和展示方式

| 数据源类型 | 来源系统 | 展示形式 | 关键字段 |
| --- | --- | --- | --- |
| 题库 | 培训考试系统 | 题目卡片 | 题干、选项、答案、难度、知识点 |
| 知识点库 | 知识管理系统 | 树形结构 | 知识点名称、层级、关联题数 |
| 考试规则库 | 规则配置系统 | 规则列表 | 题型配比、分值分配、时长标准 |
| 历史考试数据 | 统计分析系统 | 数据图表 | 正确率、易错点、区分度 |

#### 特殊交互设计点

**1. 智能难度调节**

```plaintext
当AI检测到试卷难度不均衡时：

⚠️ 难度分布建议：

当前配置：
简单题：15 题（37.5%）
中等题：18 题（45%）
困难题：7 题（17.5%）

📊 建议调整：
简单题过多，建议替换 3 道为中等题，以提高区
分度。

[应用建议] [保持当前]
```

**2. 知识点覆盖检查**

```plaintext
🔍 知识点覆盖分析：

✅ 已覆盖知识点（14个）：
• 锂电池充电安全 ✓
• 热失控应急处置 ✓
• 触电急救 ✓
• ...

⚠️ 缺失知识点（2个）：
• 防雷接地检测（重要性：中）
• 静电防护措施（重要性：低）

💡 建议补充 1-2 题以提高覆盖率
[自动补充] [忽略]
```

**3. 题目去重检查**

```plaintext
当用户多次生成试卷时：

🔔 重复题目提醒：

本次生成的试卷与以下历史试卷存在题目重复：

• 2025-12-15 电工培训考试
  重复题目：5 题（12.5%）

• 2025-11-20 电工复训考试
  重复题目：3 题（7.5%）

💡 建议操作：
[自动替换重复题] [保持原题] [查看详情]
```

**4. 一键生成答案卡和解析**

```plaintext
👤 用户：
生成答案卡和解析

🤖 AI响应：
✅ 已同步生成以下文档：

📄 已生成文档：
┌─────────────────────────────────────┐
│ 1. 考生答题卡（A4空白版）           │
│ 2. 标准答案与评分标准               │
│ 3. 逐题详细解析（含知识点说明）    │
│ 4. 阅卷统计模板（Excel）            │
└─────────────────────────────────────┘

📥 下载选项：
• [下载全部（ZIP）]
• [仅下载答案卡]
• [导入在线考试系统]
```

**5. 试卷难度预测**

```plaintext
基于历史考试数据的AI预测：

📊 预测分析：

预计通过率：76%（基于相似试卷历史数据）
预计平均分：78 分
预计考试时长：48 分钟（60%考生完成时间）

易错题预警（前3题）：
• 第8题（多选）- 历史正确率 42%
• 第15题（单选）- 历史正确率 58%
• 第23题（判断）- 历史正确率 61%

💡 建议：
如需提高区分度，可考虑调整第8题为单选题
```

---

## 通用交互规范

### 数据接入三步法

所有作品类型的数据接入遵循统一的三步流程，确保用户体验的一致性：

```plaintext
┌─────────────────────────────────────────────────┐
│                                                 │
│  Step 1: AI智能推荐                             │
│  ═══════════════════════════════════════════   │
│  • 用户发起创作需求                             │
│  • AI分析需求并识别所需数据类型                 │
│  • AI主动列出可用数据源及数据量                 │
│  • 提供快捷选项（全选/分类选择/跳过）          │
│                                                 │
├─────────────────────────────────────────────────┤
│                                                 │
│  Step 2: 用户确认选择                           │
│  ═══════════════════════════════════════════   │
│  • 用户通过自然语言或点击确认数据源             │
│  • AI加载数据并在Chat中展示预览                 │
│  • 用户可展开查看详情、筛选调整                 │
│  • 支持增删改操作                               │
│                                                 │
├─────────────────────────────────────────────────┤
│                                                 │
│  Step 3: 内容生成插入                           │
│  ═══════════════════════════════════════════   │
│  • AI基于已接入数据生成内容                     │
│  • 内容直接插入画布（干净的文档格式）          │
│  • 数据引用以脚注形式保留                       │
│  • Chat中保留数据卡片供后续调整                 │
│                                                 │
└─────────────────────────────────────────────────┘
```

### Chat内数据卡片设计规范

#### 1. 卡片基础结构

```markdown
┌─────────────────────────────────────┐
│ 📊 [图标] 数据源名称 (X条)          │  ← 头部：类型标识
├─────────────────────────────────────┤
│ • 数据条目 1 - 关键信息摘要         │  ← 内容区：默认显示前3条
│ • 数据条目 2 - 关键信息摘要         │
│ • 数据条目 3 - 关键信息摘要         │
│ ... 还有 X 条                       │
│                                     │
│ [展开全部] [筛选] [移除]            │  ← 操作区：交互按钮
└─────────────────────────────────────┘
```

#### 2. 卡片状态设计

| 状态 | 视觉表现 | 说明 |
| --- | --- | --- |
| **加载中** | 骨架屏动画 + "正在加载..." | 数据查询进行中 |
| **已加载** | 完整卡片展示 | 数据加载完成，可交互 |
| **已应用** | 绿色对勾标识 + 灰色背景 | 数据已用于内容生成 |
| **已过期** | 黄色提醒标识 + 更新按钮 | 数据源有更新 |
| **错误** | 红色警告标识 + 错误说明 | 数据加载失败 |

#### 3. 卡片交互规范

**折叠/展开：**

```plaintext
默认状态（折叠）：
┌─────────────────────────────────────┐
│ 📊 事故案例库 (8条)                 │
│ • 2023-05-12 某车间坠落事故 ▼      │
└─────────────────────────────────────┘

展开后：
┌─────────────────────────────────────┐
│ 📊 事故案例库 (8条) ▲               │
├─────────────────────────────────────┤
│ ✓ 2023-05-12 某车间坠落事故         │
│   位置：2号车间二层                 │
│   伤亡：1人重伤                     │
│   原因：未系安全带                  │
│   [查看完整报告]                     │
│                                     │
│ ✓ 2024-02-03 外包施工高空坠物       │
│   位置：3号厂房外墙                 │
│   伤亡：2人轻伤                     │
│   原因：防护措施不当                │
│   [查看完整报告]                     │
│                                     │
│ ... 还有 6 条                       │
│ [收起]                               │
└─────────────────────────────────────┘
```

**筛选功能：**

```plaintext
点击[筛选]后弹出筛选面板：

┌─────────────────────────────────────┐
│ 🔍 筛选事故案例                     │
├─────────────────────────────────────┤
│ 时间范围：                          │
│ [2023-01-01] 至 [2025-12-21]       │
│                                     │
│ 伤亡程度：                          │
│ ☐ 死亡  ☑ 重伤  ☑ 轻伤  ☐ 无伤亡   │
│                                     │
│ 所属部门：                          │
│ ☑ 全部  ☐ 生产车间  ☐ 仓储部       │
│                                     │
│ 关键词：                            │
│ [高处作业____________________]      │
│                                     │
│ [应用筛选] [重置]                    │
└─────────────────────────────────────┘
```

#### 4. 卡片类型图标规范

| 数据类型 | 图标 | 颜色 |
| --- | --- | --- |
| 事故案例 | 🔴 | 红色 |
| 法规条款 | 📜 | 蓝色 |
| 巡检记录 | 📋 | 橙色 |
| 题库数据 | 📚 | 绿色 |
| 人员信息 | 👤 | 灰色 |
| 组织架构 | 🏢 | 紫色 |
| 历史文档 | 📄 | 棕色 |
| 培训素材 | 🎬 | 青色 |

### 画布内容呈现规范

#### 1. 脚注引用方式

**基本格式：**

```markdown
## 第三章 典型事故警示

2023年5月12日，某车间发生高处坠落事故[^1]，造成1人重伤。
事故原因为作业人员未按规定系安全带，违反了《高处作业安全
规程》相关要求[^2]。

---

[^1]: 数据来源：事故案例库 > 2023-05-12 某车间坠落事故
      [在 Chat 中查看详情]
      
[^2]: 数据来源：法规库 > 高处作业安全规程 > 第3.2条
      [在 Chat 中查看详情]
```

#### 2. 内联引用方式（可选）

```markdown
根据企业组织架构（最后更新：2025-12-01），当前安全生产
责任体系包括：

• 总经理：张三（主要负责人）
• 安全总监：李四（安全管理负责人）
• 车间主任：王五、赵六（一线管理负责人）

> 💡 数据来源：HR系统 > 组织架构
> [更新数据] [查看完整架构]
```

#### 3. 图表数据引用

```markdown
### 3.2 隐患统计分析

![隐患统计图](chart_url)

> 📊 数据来源：巡检管理系统 > 2025年1-12月巡检数据
> 统计周期：2025-01-01 至 2025-12-21
> [更新数据] [查看明细]
```

### 数据溯源机制

#### 1. 一键查看数据源

```plaintext
用户在画布中点击脚注链接 [在 Chat 中查看详情]

→ Chat 自动滚动到对应的数据卡片
→ 数据卡片高亮显示 3 秒
→ 自动展开详细信息
```

#### 2. 数据变更追踪

```plaintext
当引用的数据源发生变更时：

🔔 数据更新提醒（Chat顶部）
┌─────────────────────────────────────┐
│ ⚠️ 您引用的数据已更新               │
│                                     │
│ • 事故案例库：新增 1 条记录         │
│ • 组织架构：有 2 处变更             │
│                                     │
│ 建议操作：                          │
│ [更新文档内容] [标记提醒] [忽略]   │
└─────────────────────────────────────┘
```

#### 3. 数据版本记录

```plaintext
画布底部显示数据引用版本信息：

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 数据引用记录

• 事故案例库：8 条记录（数据版本：2025-12-21 10:15）
• 法规库：5 条条款（数据版本：2024-11-15 更新）
• 组织架构：1 份（数据版本：2025-12-01 同步）

[查看完整溯源链] [导出数据清单]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 语音交互优化（针对高频场景Type-3）

针对业务单据（隐患整改通知书等）的高频使用场景，设计专门的语音交互优化方案，实现真正的"解放双手"操作。

### 语音指令识别

#### 1. 快速生成指令

| 指令类型 | 语音示例 | 触发动作 |
| --- | --- | --- |
| **直接生成** | "隐患通知书" / "整改通知" / "通知书" | 自动调取最近巡检记录 |
| **指代生成** | "刚才那个" / "最后一条" / "这个隐患" | 基于时间或上下文识别目标 |
| **批量生成** | "全部生成" / "所有隐患" | 对所有待处理记录批量生成 |

#### 2. 修改调整指令

| 指令类型 | 语音示例 | 触发动作 |
| --- | --- | --- |
| **期限调整** | "改成5天" / "延长到下周五" | 自动计算日期并更新 |
| **责任人调整** | "换成张三" / "改成设备部负责" | 匹配人员信息并更新 |
| **内容补充** | "加上部门领导" / "抄送给安全总监" | 添加相关人员 |

#### 3. 发送分发指令

| 指令类型 | 语音示例 | 触发动作 |
| --- | --- | --- |
| **快速发送** | "发出去" / "发送" / "推送" | 默认渠道发送（企业微信） |
| **指定渠道** | "发邮件" / "打印" / "发微信" | 按指定方式发送 |
| **确认状态** | "发送状态" / "对方看了吗" | 查询送达和已读状态 |

### 快速调取逻辑

#### 1. 时间窗口策略

```plaintext
用户说："生成隐患通知书"

AI执行逻辑：
1. 定位当前用户身份（安全员A）
2. 查询时间窗口：最近 2 小时
3. 筛选条件：
   - 巡检人 = 安全员A
   - 状态 = 待处理
   - 隐患记录存在
4. 按时间倒序排列
5. 返回前 3 条（超过3条时提示总数）
```

#### 2. 智能指代识别

```plaintext
场景1：连续操作
👤 10:15 "生成隐患通知书"
🤖 [展示2条待处理隐患]
👤 10:16 "第一条"
🤖 [生成通知书]
👤 10:17 "第二条也做一个"
🤖 [识别"第二条"指代上次展示的第2条记录]

场景2：指代最近
👤 "刚才那个"
🤖 [识别最近一次交互涉及的记录]

场景3：模糊指代
👤 "那个仓库的"
🤖 [基于关键词"仓库"匹配相关记录]
```

#### 3. 优先级排序

当匹配到多条记录时，按以下优先级排序：

1. **隐患等级**：重大 > 较大 > 一般

2. **时间紧迫度**：超期 > 临期 > 正常

3. **发现时间**：最新优先

4. **区域归属**：当前位置优先（基于定位）

### 语音播报设计

#### 1. 简洁模式（默认）

```plaintext
🔊 语音播报内容：

"找到2条待处理隐患。
第一条，仓库消防器材遮挡，责任人李明；
第二条，车间电气线路老化，责任人张伟。
请选择哪一条？"

特点：
• 仅播报关键信息（位置、隐患、责任人）
• 省略次要细节（发现时间、等级等）
• 语速适中，清晰易懂
```

#### 2. 详细模式（可选）

```plaintext
用户说："详细说一下"

🔊 语音播报：

"第一条隐患详情：
发现时间，今天上午10点15分；
位置，3号仓库西侧疏散通道；
隐患描述，消防器材被货物遮挡；
隐患等级，一般隐患；
责任人，仓储部李明，工号1234；
建议整改期限，3个工作日。
需要查看现场照片吗？"
```

#### 3. 确认反馈

```plaintext
生成完成后的语音反馈：

🔊 "通知书已生成，文号YH-2025-1221-001，
   已发送给李明。需要我读一下通知内容吗？"

用户说"不用"：
🔊 "好的，您可以随时在画布中查看。"

用户说"读一下"：
🔊 "隐患整改通知书，致仓储部李明...（读取正文）"
```

#### 4. 环境噪音处理

**降噪策略：**

- 自动识别环境噪音（如机器轰鸣）

- 当识别失败率 > 30% 时，自动切换到文字确认模式

- 提示："检测到环境噪音较大，建议使用文字输入或移步安静区域"

**唤醒词优化：**

```plaintext
标准唤醒词："小安" / "安全助手"
现场快捷词："生成" / "通知书" / "发送"（无需唤醒直接识别）
```

#### 5. 多轮对话优化

```plaintext
连续语音交互示例：

👤 "生成隐患通知书"
🤖 "找到2条，第一条仓库消防器材，第二条车间电气线路"
👤 "第一条"
🤖 "已生成，发送吗？"
👤 "发送"
🤖 "已发送给李明"
👤 "第二条也做"
🤖 "已生成第二条，发送吗？"
👤 "发"
🤖 "已发送给张伟，两条都完成了"
```

---

## 技术实现要点

### Chat数据接入能力需求

#### 1. 智能意图识别

**需求说明：**\
AI需要准确识别用户的创作意图，并推断所需的数据类型。

**技术实现：**

```python
# 意图识别模型输入
user_input = "帮我生成一份高处作业安全培训课件"

# 模型输出
{
  "intent": "create_courseware",  # 创作意图
  "subject": "高处作业安全",      # 主题
  "required_data": [
    {
      "type": "accident_case",    # 事故案例
      "keywords": ["高处作业", "坠落"],
      "priority": "high"
    },
    {
      "type": "regulation",       # 法规
      "keywords": ["高处作业", "安全规范"],
      "priority": "high"
    },
    {
      "type": "sop",              # 作业流程
      "keywords": ["高处作业", "标准流程"],
      "priority": "medium"
    }
  ]
}
```

#### 2. 实时数据查询

**需求说明：**\
根据识别出的数据需求，实时调用业务系统API获取数据。

**API调用规范：**

```javascript
// 统一数据查询接口
async function fetchData(params) {
  const response = await apiClient.post('/api/data/query', {
    dataSource: params.type,        // 数据源类型
    keywords: params.keywords,       // 搜索关键词
    filters: params.filters,         // 筛选条件
    limit: params.limit || 10,       // 返回数量
    offset: params.offset || 0       // 分页偏移
  });
  
  return {
    total: response.data.total,      // 总记录数
    items: response.data.items,      // 数据列表
    timestamp: response.data.timestamp // 数据时间戳
  };
}

// 使用示例
const accidentCases = await fetchData({
  type: 'accident_case',
  keywords: ['高处作业', '坠落'],
  filters: {
    dateRange: ['2023-01-01', '2025-12-21'],
    severity: ['重伤', '死亡']
  }
});
```

#### 3. 数据预览渲染

**需求说明：**\
在Chat中以卡片形式展示数据，支持折叠/展开、筛选等交互。

**前端组件设计：**

```plaintext
<template>
  <div class="data-card">
    <div class="card-header" @click="toggleExpand">
      <icon :type="dataIcon">
      <span>{{ dataSource }} ({{ total }}条)</span>
      <icon :type="expandIcon">
    </icon></icon></div>
    
    <div v-if="expanded" class="card-body">
      <div v-for="item in displayItems" :key="item.id" class="data-item">
        <div class="item-summary">{{ item.summary }}</div>
        <div class="item-actions">
          <button @click="viewDetail(item)">查看详情</button>
        </div>
      </div>
      
      <div v-if="total > displayLimit" class="more-info">
        ... 还有 {{ total - displayLimit }} 条
      </div>
      
      <div class="card-actions">
        <button @click="expandAll">展开全部</button>
        <button @click="openFilter">筛选</button>
        <button @click="removeData">移除</button>
      </div>
    </div>
  </div>
</template>

<script>
export default {
  props: {
    dataSource: String,    // 数据源名称
    total: Number,         // 总数量
    items: Array           // 数据列表
  },
  data() {
    return {
      expanded: false,
      displayLimit: 3
    };
  },
  computed: {
    displayItems() {
      return this.items.slice(0, this.displayLimit);
    }
  }
};
</script>
```

#### 4. 状态持久化

**需求说明：**\
已接入的数据在会话期间保持可见，支持随时查看和调整。

**状态管理：**

```javascript
// 使用Vuex管理数据状态
const store = {
  state: {
    sessionData: {
      conversationId: '',
      dataCards: [],      // 已接入的数据卡片
      appliedData: [],    // 已应用于生成的数据
      timestamp: null
    }
  },
  
  mutations: {
    ADD_DATA_CARD(state, card) {
      state.sessionData.dataCards.push(card);
    },
    
    REMOVE_DATA_CARD(state, cardId) {
      state.sessionData.dataCards = state.sessionData.dataCards
        .filter(card => card.id !== cardId);
    },
    
    UPDATE_DATA_CARD(state, { cardId, updates }) {
      const card = state.sessionData.dataCards
        .find(card => card.id === cardId);
      if (card) {
        Object.assign(card, updates);
      }
    },
    
    MARK_AS_APPLIED(state, cardId) {
      state.sessionData.appliedData.push(cardId);
    }
  }
};
```

### API接口要求

#### 1. 数据查询接口

**接口定义：**

```plaintext
POST /api/data/query
Content-Type: application/json

Request Body:
{
  "dataSource": "accident_case",      // 数据源类型
  "keywords": ["高处作业"],           // 搜索关键词
  "filters": {                        // 筛选条件
    "dateRange": ["2023-01-01", "2025-12-21"],
    "severity": ["重伤", "死亡"],
    "department": ["生产车间"]
  },
  "sort": {                           // 排序
    "field": "occurTime",
    "order": "desc"
  },
  "pagination": {                     // 分页
    "page": 1,
    "pageSize": 10
  }
}

Response:
{
  "code": 200,
  "data": {
    "total": 8,
    "items": [
      {
        "id": "ACC-2023-001",
        "title": "某车间坠落事故",
        "occurTime": "2023-05-12 14:30:00",
        "location": "2号车间二层",
        "severity": "重伤",
        "casualties": "1人重伤",
        "summary": "作业人员未系安全带导致坠落",
        "photos": ["url1", "url2"],
        "reportUrl": "/reports/ACC-2023-001"
      },
      // ... 更多记录
    ],
    "timestamp": "2025-12-21T08:30:00Z"
  }
}
```

#### 2. 数据详情接口

```plaintext
GET /api/data/detail/{dataSource}/{id}

Response:
{
  "code": 200,
  "data": {
    "id": "ACC-2023-001",
    "basicInfo": {
      "title": "某车间坠落事故",
      "occurTime": "2023-05-12 14:30:00",
      "location": "2号车间二层平台",
      "discoverer": "安全员王明"
    },
    "casualtyInfo": {
      "casualties": "1人重伤",
      "injuredPersons": [
        {
          "name": "李强",
          "age": 32,
          "position": "维修工",
          "injuryLevel": "重伤",
          "injuryDescription": "腰椎骨折"
        }
      ]
    },
    "causeAnalysis": {
      "directCause": "未系安全带",
      "indirectCause": "安全意识淡薄，现场监管不到位",
      "responsiblePerson": "车间主任张伟"
    },
    "attachments": {
      "photos": ["url1", "url2", "url3"],
      "videos": ["url4"],
      "reports": ["url5"]
    },
    "metadata": {
      "createTime": "2023-05-12 15:00:00",
      "updateTime": "2023-05-15 10:30:00",
      "version": "1.2"
    }
  }
}
```

#### 3. 数据更新通知接口（WebSocket）

```plaintext
WebSocket: wss://api.example.com/ws/data-updates

Client → Server (订阅):
{
  "action": "subscribe",
  "dataSource": "accident_case",
  "filters": {
    "keywords": ["高处作业"]
  }
}

Server → Client (推送):
{
  "event": "data_updated",
  "dataSource": "accident_case",
  "changes": {
    "added": [
      {
        "id": "ACC-2025-010",
        "title": "新增高空坠物事故",
        "occurTime": "2025-12-21 09:30:00"
      }
    ],
    "modified": [],
    "deleted": []
  },
  "timestamp": "2025-12-21T09:35:00Z"
}
```

#### 4. 批量数据获取接口

```plaintext
POST /api/data/batch

Request Body:
{
  "requests": [
    {
      "dataSource": "accident_case",
      "ids": ["ACC-2023-001", "ACC-2024-005"]
    },
    {
      "dataSource": "regulation",
      "ids": ["REG-2021-001"]
    }
  ]
}

Response:
{
  "code": 200,
  "data": {
    "accident_case": [
      { "id": "ACC-2023-001", /* 详细数据 */ },
      { "id": "ACC-2024-005", /* 详细数据 */ }
    ],
    "regulation": [
      { "id": "REG-2021-001", /* 详细数据 */ }
    ]
  }
}
```

### 数据状态管理

#### 1. 会话级状态管理

**需求说明：**\
在单次对话会话中，已接入的数据需要持久化，直到用户关闭会话或主动清除。

**实现方案：**

```javascript
class SessionDataManager {
  constructor(conversationId) {
    this.conversationId = conversationId;
    this.dataStore = new Map();  // 数据卡片存储
    this.appliedIds = new Set(); // 已应用的数据ID
    this.loadFromStorage();
  }
  
  // 添加数据卡片
  addDataCard(card) {
    this.dataStore.set(card.id, {
      ...card,
      addedAt: Date.now(),
      status: 'loaded'
    });
    this.saveToStorage();
  }
  
  // 标记为已应用
  markAsApplied(cardId) {
    const card = this.dataStore.get(cardId);
    if (card) {
      card.status = 'applied';
      card.appliedAt = Date.now();
      this.appliedIds.add(cardId);
      this.saveToStorage();
    }
  }
  
  // 移除数据卡片
  removeDataCard(cardId) {
    this.dataStore.delete(cardId);
    this.appliedIds.delete(cardId);
    this.saveToStorage();
  }
  
  // 持久化到本地存储
  saveToStorage() {
    const data = {
      conversationId: this.conversationId,
      cards: Array.from(this.dataStore.values()),
      appliedIds: Array.from(this.appliedIds),
      timestamp: Date.now()
    };
    localStorage.setItem(
      `session_data_${this.conversationId}`, 
      JSON.stringify(data)
    );
  }
  
  // 从本地存储加载
  loadFromStorage() {
    const stored = localStorage.getItem(
      `session_data_${this.conversationId}`
    );
    if (stored) {
      const data = JSON.parse(stored);
      data.cards.forEach(card => this.dataStore.set(card.id, card));
      data.appliedIds.forEach(id => this.appliedIds.add(id));
    }
  }
}
```

#### 2. 数据版本管理

**需求说明：**\
跟踪数据的版本变化，当引用的数据源更新时，提醒用户。

**实现方案：**

```javascript
class DataVersionManager {
  constructor() {
    this.versionCache = new Map();
  }
  
  // 记录数据版本
  recordVersion(dataId, version) {
    this.versionCache.set(dataId, {
      version,
      recordedAt: Date.now()
    });
  }
  
  // 检查版本更新
  async checkUpdates(dataIds) {
    const updates = [];
    
    for (const id of dataIds) {
      const cached = this.versionCache.get(id);
      if (!cached) continue;
      
      // 查询最新版本
      const latest = await this.fetchLatestVersion(id);
      
      if (latest.version !== cached.version) {
        updates.push({
          dataId: id,
          oldVersion: cached.version,
          newVersion: latest.version,
          changes: latest.changes
        });
      }
    }
    
    return updates;
  }
  
  // 获取最新版本
  async fetchLatestVersion(dataId) {
    const response = await apiClient.get(`/api/data/version/${dataId}`);
    return response.data;
  }
}
```

#### 3. 数据缓存策略

**需求说明：**\
为提升性能，对频繁访问的数据进行缓存，设定合理的缓存失效策略。

**缓存策略：**

```javascript
class DataCacheManager {
  constructor() {
    this.cache = new Map();
    this.cacheTTL = {
      'accident_case': 10 * 60 * 1000,     // 10分钟
      'regulation': 24 * 60 * 60 * 1000,   // 24小时
      'inspection': 5 * 60 * 1000,         // 5分钟（实时性要求高）
      'personnel': 30 * 60 * 1000,         // 30分钟
      'organization': 60 * 60 * 1000       // 1小时
    };
  }
  
  // 获取缓存数据
  async get(dataSource, id) {
    const cacheKey = `${dataSource}:${id}`;
    const cached = this.cache.get(cacheKey);
    
    // 缓存命中且未过期
    if (cached && !this.isExpired(cached, dataSource)) {
      return cached.data;
    }
    
    // 缓存未命中或已过期，重新获取
    const data = await this.fetchData(dataSource, id);
    this.set(dataSource, id, data);
    return data;
  }
  
  // 设置缓存
  set(dataSource, id, data) {
    const cacheKey = `${dataSource}:${id}`;
    this.cache.set(cacheKey, {
      data,
      cachedAt: Date.now()
    });
  }
  
  // 检查是否过期
  isExpired(cached, dataSource) {
    const ttl = this.cacheTTL[dataSource] || 10 * 60 * 1000; // 默认10分钟
    return Date.now() - cached.cachedAt > ttl;
  }
  
  // 清除缓存
  invalidate(dataSource, id) {
    const cacheKey = `${dataSource}:${id}`;
    this.cache.delete(cacheKey);
  }
}
```

### 安全性考虑

#### 1. 数据权限控制

**需求说明：**\
确保用户只能访问其有权限的数据，防止越权访问。

**实现方案：**

```javascript
// 后端权限验证中间件
async function checkDataPermission(req, res, next) {
  const { dataSource, filters } = req.body;
  const userId = req.user.id;
  
  // 获取用户权限
  const permissions = await getUserPermissions(userId);
  
  // 检查数据源访问权限
  if (!permissions.dataSources.includes(dataSource)) {
    return res.status(403).json({
      code: 403,
      message: '无权访问该数据源'
    });
  }
  
  // 检查数据范围权限（如部门隔离）
  if (permissions.dataScope === 'department') {
    filters.department = permissions.departmentId;
  }
  
  next();
}

// 前端权限检查
function filterAvailableDataSources(allSources, userPermissions) {
  return allSources.filter(source => 
    userPermissions.dataSources.includes(source.type)
  );
}
```

#### 2. 敏感数据脱敏

**需求说明：**\
对人员姓名、联系方式等敏感信息进行脱敏处理。

**脱敏规则：**

```javascript
const maskingRules = {
  // 姓名脱敏：张三 → 张*
  name: (value) => {
    if (!value || value.length < 2) return value;
    return value[0] + '*'.repeat(value.length - 1);
  },
  
  // 手机号脱敏：13812345678 → 138****5678
  mobile: (value) => {
    if (!value || value.length !== 11) return value;
    return value.substring(0, 3) + '****' + value.substring(7);
  },
  
  // 身份证脱敏：110101199001011234 → 110101********1234
  idCard: (value) => {
    if (!value || value.length < 18) return value;
    return value.substring(0, 6) + '********' + value.substring(14);
  }
};

// 应用脱敏
function applyMasking(data, userRole) {
  // 仅对非管理员用户脱敏
  if (userRole === 'admin') return data;
  
  return {
    ...data,
    name: maskingRules.name(data.name),
    mobile: maskingRules.mobile(data.mobile),
    idCard: maskingRules.idCard(data.idCard)
  };
}
```

#### 3. 数据传输加密

**需求说明：**\
确保数据在传输过程中的安全性。

**实现方案：**

- 所有API通信使用HTTPS

- WebSocket连接使用WSS

- 敏感数据字段额外加密

```javascript
// 前端加密工具
import CryptoJS from 'crypto-js';

const SECRET_KEY = process.env.VUE_APP_DATA_ENCRYPT_KEY;

function encryptSensitiveData(data) {
  const dataStr = JSON.stringify(data);
  return CryptoJS.AES.encrypt(dataStr, SECRET_KEY).toString();
}

function decryptSensitiveData(encrypted) {
  const bytes = CryptoJS.AES.decrypt(encrypted, SECRET_KEY);
  return JSON.parse(bytes.toString(CryptoJS.enc.Utf8));
}
```

#### 4. 数据引用审计

**需求说明：**\
记录数据的访问和使用情况，便于事后审计。

**审计日志：**

```javascript
class DataAuditLogger {
  async log(action, details) {
    await apiClient.post('/api/audit/log', {
      userId: getCurrentUser().id,
      action,                       // 'view', 'use', 'export'
      dataSource: details.dataSource,
      dataId: details.dataId,
      context: {
        conversationId: details.conversationId,
        documentId: details.documentId,
        purpose: details.purpose   // '生成培训课件', '编写隐患通知'
      },
      timestamp: Date.now(),
      ip: details.ip,
      userAgent: navigator.userAgent
    });
  }
}

// 使用示例
auditLogger.log('use', {
  dataSource: 'accident_case',
  dataId: 'ACC-2023-001',
  conversationId: 'conv-123',
  documentId: 'doc-456',
  purpose: '生成高处作业培训课件',
  ip: '192.168.1.100'
});
```

---

## 对比传统方案的优势

### 用户体验提升

| 对比维度 | 传统方案（画布中心） | 新方案（Chat中心） | 提升效果 |
| --- | --- | --- | --- |
| **操作流程** | 顶部菜单→选择器弹窗→画布插入→Chat生成 | Chat对话→数据确认→直接生成 | 减少3-4步操作 |
| **视觉焦点** | 需在工具栏/弹窗/画布/Chat间切换 | 始终聚焦Chat单一界面 | 减少80%注意力切换 |
| **画布整洁度** | 插入大量数据卡片，占用版面 | 始终保持专业文档外观 | 视觉干扰降低100% |
| **学习曲线** | 需理解工具栏按钮、选择器交互 | 自然语言对话，零学习成本 | 新手上手时间缩短90% |
| **移动端适配** | 工具栏和选择器难以操作 | Chat界面移动端友好 | 移动端可用性提升200% |

### 效率提升

| 场景 | 传统方案耗时 | 新方案耗时 | 效率提升 |
| --- | --- | --- | --- |
| **培训课件生成** | 60-90分钟 | 15-20分钟 | 提升75% |
| **标准公文编写** | 120-180分钟 | 30-40分钟 | 提升78% |
| **业务单据生成** | 10-15分钟 | 1-2分钟 | 提升90% |
| **考试试卷组卷** | 30-45分钟 | 3-5分钟 | 提升89% |

**效率提升来源分析：**

1. **数据查找时间**

   - 传统：需手动在多个系统中搜索、筛选

   - 新方案：AI自动推荐，1秒完成

2. **数据整理时间**

   - 传统：需复制粘贴、格式调整

   - 新方案：直接生成标准格式内容

3. **内容生成时间**

   - 传统：基于数据手动编写

   - 新方案：AI自动生成，人工仅需审核

4. **迭代调整时间**

   - 传统：修改需重新操作全流程

   - 新方案：Chat中直接说明需求即可

### 学习成本降低

#### 1. 自然语言交互

```plaintext
传统方案：
用户需要学习：
✗ "插入数据"按钮在哪里
✗ 各种数据源对应什么图标
✗ 筛选器的各个字段含义
✗ 如何删除已插入的数据卡片

新方案：
用户只需说：
✓ "帮我生成一份培训课件"
✓ "引入事故案例"
✓ "换一个案例"
```

#### 2. 智能推荐引导

```plaintext
传统方案：
用户需要自己判断：
✗ 我应该选择哪些数据源？
✗ 需要多少条数据合适？
✗ 哪些法规条款相关？

新方案：
AI主动引导：
✓ "建议引入以下数据..."
✓ "推荐保留4-5条典型案例"
✓ "找到5条相关法规，推荐度★★★★★"
```

#### 3. 错误处理友好

```plaintext
传统方案：
出错时：
✗ 显示错误代码："ERR_DATA_FETCH_FAILED"
✗ 用户不知道如何解决

新方案：
AI解释：
✓ "抱歉，事故案例库暂时无法访问，您可以：
   1. 稍后再试
   2. 先不引入案例，直接生成
   3. 从历史文档复用案例"
```

### 业务价值提升

#### 1. 内容质量保障

**数据准确性：**

- 传统：手动复制粘贴，容易出错

- 新方案：直接从业务系统获取，自动更新

**合规性保障：**

- 传统：可能引用过期法规

- 新方案：实时检查法规有效性，自动提醒更新

**一致性保障：**

- 传统：不同人编写风格差异大

- 新方案：基于统一模板生成，保持一致性

#### 2. 可追溯性

**数据溯源：**

```plaintext
传统方案：
文档中的数据来源不明
✗ 这个事故案例是从哪里来的？
✗ 这条法规是什么时候更新的？
✗ 这个责任人信息是否最新？

新方案：
每条数据都有完整溯源链
✓ 点击脚注即可查看数据源
✓ 显示数据版本和更新时间
✓ 支持导出数据清单
```

#### 3. 协作效率

**团队协作：**

```plaintext
传统方案：
✗ 数据分散在各人电脑
✗ 需要邮件/消息传递素材
✗ 版本管理混乱

新方案：
✓ 统一从系统获取数据
✓ Chat记录可共享
✓ 数据自动同步最新版本
```

### 量化对比总结

| 关键指标 | 传统方案 | 新方案 | 提升幅度 |
| --- | --- | --- | --- |
| **平均完成时间** | 60分钟 | 15分钟 | ↓ 75% |
| **操作步骤数** | 15-20步 | 3-5步 | ↓ 78% |
| **错误率** | 8-12% | <2% | ↓ 83% |
| **新手培训时间** | 4小时 | 0.5小时 | ↓ 88% |
| **移动端可用性** | 30分 | 85分 | ↑ 183% |
| **用户满意度** | 65分 | 92分 | ↑ 42% |

---

## 总结

本方案通过将数据接入流程整合到Chat对话中，实现了：

✅ **单一交互界面**：用户始终聚焦Chat，无需在多个界面切换\
✅ **自然语言操作**：零学习成本，支持语音输入\
✅ **智能推荐引导**：AI主动分析需求并推荐数据源\
✅ **画布保持简洁**：最终文档专业整洁，无中间状态干扰\
✅ **数据完整溯源**：所有引用数据可追溯，支持实时更新\
✅ **高频场景优化**：针对业务单据等场景，支持1-2分钟极速生成

该方案特别适合安全生产领域的专业用户，兼顾效率、准确性和易用性，可显著提升内容创作的质量和效率。